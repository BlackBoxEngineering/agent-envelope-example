# Shared Runtime Helpers

These files are used by the example scenarios:

- `config.js` loads `.env.local` from the repo root and exposes required environment values.
- `hosted.js` wraps the hosted API routes used by the examples.
- `preflight.js` validates local hosted-governance environment setup.

They are not standalone examples.
