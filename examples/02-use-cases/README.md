# Offline Use Cases

## Abstract

This shows that an AgentEnvelope "agent" is any bounded actor, not only an AI chatbot. The same derive, sign, and verify primitive is used for support messaging, a hotel room key, and drone dispatch.

## Run

```bash
npm run use:cases
```

## Expected Result

Each example prints its operation, resources, derived agent address, and a successful local verification.
