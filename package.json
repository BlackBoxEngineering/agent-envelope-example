{
  "name": "agent-envelope-example",
  "version": "2.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "description": "Integration examples for agent-envelope-sdk: sovereign offline flow (no account, no key, no network) and portal-governed flow (mint delegate, hosted verify, MCP server).",
  "scripts": {
    "test": "node --test test.js",
    "sovereign": "node sovereign.js",
    "portal:setup": "node portal-setup.js",
    "preflight": "node preflight.js",
    "bot": "node preflight.js && node bot.js",
    "verifier": "node verifier.js",
    "escape:incidents": "node escape-incidents.js",
    "demo": "node preflight.js && node bot.js && node verifier.js",
    "mcp": "node mcp-server.js"
  },
  "dependencies": {
    "agent-envelope-mcp": "^1.0.3",
    "agent-envelope-sdk": "^1.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
