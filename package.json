{
  "name": "agent-envelope-example",
  "version": "2.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "description": "Vault-anchored example: a registered bot mints through the vault API and a verifier checks authority via the API. No local vault, no passphrase, no local root.",
  "scripts": {
    "sovereign": "node sovereign.js",
    "bot": "node bot.js",
    "verifier": "node verifier.js",
    "demo": "node bot.js && node verifier.js",
    "mcp": "node mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "agent-envelope-sdk": "^1.3.0",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=18"
  }
}
